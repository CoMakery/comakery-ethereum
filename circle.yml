machine:
  node:
    version: 6

dependencies:
  override:
    - yarn install
  cache_directories:
    - ~/.yarn
    - ~/.cache/yarn

test:
  pre:
    - yarn run testrpc:
        background: true
  override:
    - yarn ci
