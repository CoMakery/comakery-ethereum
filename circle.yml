machine:
  node:
    version: 6

dependencies:
  pre:
    npm i -g yarn
  override:
    - yarn
  cache_directories:
    - ~/.yarn
    - ~/.cache/yarn

test:
  pre:
    - yarn testrpc:
        background: true
  override:
    - yarn ci
