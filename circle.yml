machine:
  timezone:
    America/Los_Angeles

dependencies:
  pre:
    - nvm install 5.6
    - nvm alias default 5.6
    - nvm use default

test:
  pre:
    - npm run testrpc:
        background: true
  override:
    - npm run fulltest
